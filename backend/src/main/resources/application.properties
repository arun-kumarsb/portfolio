# ===================================================================
# Arun's Full-Stack Portfolio Application Configuration
# ===================================================================

# Server Port
server.port=8080

# Spring Application Name
spring.application.name=portfolio-backend

# Database Configuration (PostgreSQL with Environment Variable Overrides)
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-da4ls5on74is73e0rsvg-a:5432/portfolio_db_mvyr}
spring.datasource.username=${DB_USERNAME:arun}
spring.datasource.password=${DB_PASSWORD:arun}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect 
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Admin Security Credentials (Customize your login details here)
app.admin.username=${APP_ADMIN_USERNAME:arun}
app.admin.password=${APP_ADMIN_PASSWORD:1yurSmilx*}

# JWT Token Security Settings
app.jwt.secret=${APP_JWT_SECRET:ArunPortfolioSecureJwtSecretKeyForSpringSecurityAuthentication2026!}
app.jwt.expiration-ms=86400000

# Jackson JSON Serialization
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
spring.jackson.time-zone=UTC
